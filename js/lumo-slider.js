jQuery(function($){
	$('#lumo-slider').plusSlider({
		createArrows: true,
		fullWidth: true,
		createPagination: false,
		sliderType: 'slider', //'slider' or 'fader'
		infiniteSlide: true,
		displayTime: 6000
	});
});
